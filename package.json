{
  "name": "server",
  "version": "1.0.0",
  "description": "* npm init -y * Add env * add config based on development environments * Error handling for loading env * Add Constants for variables, messages etc. * Connect to MongoDB using mongoose",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "method-override": "^3.0.0",
    "mongoose": "^5.8.1",
    "request": "^2.88.0",
    "request-promise": "^4.2.5",
    "stellar-sdk": "^3.3.0",
    "winston": "^3.2.1",
    "winston-daily-rotate-file": "^4.4.0"
  }
}
